import secondsToDisplayLength from 'showtime/utils/seconds-to-display-length';
import { module, test } from 'qunit';

module('Unit | Utility | seconds to display length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = secondsToDisplayLength();
  assert.ok(result);
});
